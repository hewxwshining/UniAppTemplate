/// <reference types="@dcloudio/types" />
import { msg } from '@/utils/base';
import { clearToken, getAccessToken } from './cache';
import { CODE_NOARG, CODE_NOBINPER, CODE_NODATA } from '@/api/const';
import { useUserStore } from '@/store';

// 定义请求配置接口
export interface RequestOptions {
	url : string;
	method ?: 'GET' | 'POST' | 'PUT' | 'DELETE';
	data ?: any;
	header ?: Record<string, string>;
	loading ?: boolean;
	loadingText ?: string;
}

// 定义响应数据接口
export interface ResponseData<T = any> {
	begtimestamp : string;
	code : number;
	data : T;
	endtimestamp : string;
	/** 可选：接口是否成功的标记 */
	msg : string;
}

// 根据项目配置获取基础URL
export const getBaseUrl = () => {
	return 'http://your-production-server-ip:port';
	// return 'http://localhost:9001'; // 本地开发环境
};

// ... [以下代码保持不变]

